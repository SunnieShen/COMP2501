---
title: "Learning Data Science with R"
author: "CS2501"
date: "`r Sys.Date()`"
output:
  pdf_document: default
  html_document: default
---

# Learning Data Science with R

Welcome to the **ultimate guide** to learning **Data Science** with **R**. This guide is designed to help you master data science concepts while learning R, one of the most popular programming languages for data analysis and visualization.

------------------------------------------------------------------------

## Table of Contents

1.  [Introduction](#introduction)
2.  [What is Data Science?](#what-is-data-science)
3.  [Why Learn R?](#why-learn-r)
4.  [Key Topics](#key-topics)
    -   [Programming with R](#programming-with-r)
    -   [Data Visualization in R](#data-visualization-in-r)
    -   [Statistics and Probability](#statistics-and-probability)
    -   [Statistical Models and Machine Learning](#statistical-model-and-machine-learning)
5.  [Our Progress](#our-progress)
6.  [Resources](#resources)
7.  [Conclusion](#conclusion)

---
## Introduction

Data Science is a multidisciplinary field that combines **mathematics**, **statistics**, **computer science**, and **domain knowledge** to extract insights from data.

> *"R is not just a statistical language; it's a complete ecosystem for data science."*
---

## What is Data Science? {#what-is-data-science}

Data science involves five key steps:

1.  **Data Collection**
2.  **Data Cleaning**
3.  **Exploratory Data Analysis (EDA)**
4.  **Model Building**
5.  **Data Visualization and Communication**

------------------------------------------------------------------------

## Why Learn R? {#why-learn-r}

R is a **powerful language** for statistical analysis, data visualization, and machine learning. Some benefits include:

-   Excellent libraries for data science (e.g., `dplyr`, `ggplot2`).
-   Strong community support.
-   Built-in tools for statistical modeling.
-   Seamless integration with visualization libraries.

![**R packages for data science**](https://www.business-science.io/assets/069_r_logo_board.png)

Here is an example of creating a bar chart in R:

```{r, echo=TRUE}
# Load the ggplot2 library
library(ggplot2)

# Create a simple dataset
data <- data.frame(
  Category = c("A", "B", "C"),
  Values = c(10, 20, 15)
)

# Plot a bar chart
data |> ggplot(aes(x = Category, y = Values)) +
  geom_bar(stat = "identity") +
  theme_minimal()
```

------------------------------------------------------------------------

## Key Topics {#key-topics}

### Programming with R {#programming-with-r}

R is the primary language for this course. Key concepts include:

-   **Data Structures**: Vectors, matrices, lists, and data frames.
-   **Data Manipulation**: Using `tidyverse` to filter, group, and summarize data.
-   **Writing Functions**: Creating reusable code.

#### Example Table of Data Structures in R

| **Structure** | **Description** | **Example** |
|----|----|----|
| **Vector** | A sequence of data elements of the same basic type. | `c(1, 2, 3)` |
| **Matrix** | A 2D array where all elements are of the same type. | `matrix(1:6, nrow = 2, ncol = 3)` |
| **List** | A collection of elements that can be of different types. | `list(a = 1, b = "text", c = TRUE)` |
| **Data Frame** | A table or two-dimensional array-like structure. | `data.frame(x = 1:3, y = c("a", "b", "c"))` |

Example:

```{r, echo=TRUE}
# Create a vector
numbers <- c(1, 2, 3, 4, 5)

# Calculate the mean
mean(numbers)
```

------------------------------------------------------------------------

### Statistics and Probability {#statistics-and-probability}

R is excellent for statistical analysis. Topics include:

-   Probability Theories
-   Probability Distributions and Statistical Inference
-   Hypothesis Testing
-   Bayesian Statistics
-   Regression Analysis

Example:

```{r, echo=TRUE}

# Perform linear regression
data(mtcars)
model <- lm(mpg ~ wt, data = mtcars)

# Display a summary of the regression model
summary(model)
```

------------------------------------------------------------------------

### Data Visualization in R {#data-visualization-in-r}

Visualization is a critical skill in data science. R offers powerful libraries like:

-   **ggplot2**: For creating beautiful graphics.
-   **plotly**: For interactive visualizations.

Here’s an example of a scatter plot:

```{r, echo=TRUE}
# Scatter plot with ggplot2
plot(mtcars$wt, mtcars$mpg, 
     main = "Linear Regression: MPG vs Weight",
     xlab = "Car Weight",
     ylab = "Miles Per Gallon",
     pch = 19, col = "blue")

# Add the regression line
abline(model, col = "red", lwd = 2)
```

------------------------------------------------------------------------

### Statistical Models and Machine Learning

R has a variety of machine learning packages, such as:

-   **caret**
-   **randomForest**
-   **xgboost**

Steps in building a machine learning model:

1.  Split the dataset into training and testing sets.
2.  Train a model using algorithms like linear regression or decision trees.
3.  Evaluate the model's performance using metrics like accuracy or RMSE.

Example:

```{r, echo=TRUE, warning=FALSE}
# Split data into training and testing sets
library(caret)
set.seed(123)
split <- createDataPartition(mtcars$mpg, p = 0.8, list = FALSE)
train <- mtcars[split, ]
test <- mtcars[-split, ]

# Train a linear regression model
model <- train(mpg ~ ., data = train, method = "lm")

# Summarize the model
summary(model)
```

------------------------------------------------------------------------

## Our Progress {#our-progress}

-   [x] Set up R and RStudio
-   [x] Learn basic R syntax
-   [ ] Learn data manipulation with `dplyr`
-   [ ] Explore data visualization with `ggplot2`
-   [ ] Build your data science project

------------------------------------------------------------------------

## Resources {#resources}

Here are some recommended resources to learn R and data science:

1.  **Books**
    -   *Introduction to Data Science* by Rafael A. Irizarry
    -   *R for Data Science* by Hadley Wickham and Garrett Grolemund.
2.  **Cheat Sheets**
    -   [RStudio Cheat Sheets](https://www.rstudio.com/resources/cheatsheets/)
3.  **Communities**
    -   [RStudio Community](https://community.rstudio.com)
    -   [Stack Overflow](https://stackoverflow.com)

------------------------------------------------------------------------

## Conclusion {#conclusion}

R is an indispensable tool for data scientists. By mastering it, you'll gain the ability to analyze, visualize, and model data effectively.
